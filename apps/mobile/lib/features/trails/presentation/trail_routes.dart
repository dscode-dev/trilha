/// Route identifiers for trails, kept beside the feature that owns them.
abstract final class TrailRoutes {
  const TrailRoutes._();

  static const String listPath = '/trails';
  static const String listName = 'my-trails';
}
